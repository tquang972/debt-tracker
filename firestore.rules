rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ONLY allow access for the owner's email address
    match /{document=**} {
      allow read, write: if request.auth != null
                         && request.auth.token.email == 'tquang972@gmail.com';
    }
  }
}
